# GitHub Issues — Arabic Semantic Locator Healing

## Issue 1 — Expand Arabic action dictionary
Add more domain-specific Arabic actions for government, banking, inspection, and CRM systems.

## Issue 2 — Add configurable project-level Arabic synonyms
Allow teams to define custom action aliases in `qatra.properties` or JSON.

## Issue 3 — Add component-aware Arabic semantic healing
Support dropdowns, modal buttons, table row actions, and toast messages using Arabic business intent.

## Issue 4 — Export matched Arabic synonym in healing reports
Show which Arabic phrase triggered the semantic match.

## Issue 5 — Add false-positive regression tests
Ensure `reject`, `delete`, `cancel`, and `approve` actions do not heal to each other accidentally.
